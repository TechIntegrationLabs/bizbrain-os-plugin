---
name: gsd
description: GSD project execution — plan, execute, and track structured project work
argument-hint: [new|requirements|roadmap|plan|execute|status]
---

Invoke the `bizbrain-os:gsd-workflow` skill and follow it.

**Arguments:** $ARGUMENTS

If no arguments, show project status and suggest the next action.
