---
name: intake
description: Process files in the brain's intake dump folder
argument-hint: [status|process|clear]
---

Invoke the `bizbrain-os:intake-processing` skill and follow it.

- `/intake` — Show what's in the intake dump
- `/intake process` — Process all unprocessed files
- `/intake clear` — Archive processed files

**Arguments:** $ARGUMENTS
